import React,{useRef,useState}from"react";import{Sparkles,Square}from"lucide-react";import{ai}from"../lib/api";export default function AI({done}){let[on,setOn]=useState(false),[busy,setBusy]=useState(false),rec=useRef();function speak(t){if(speechSynthesis){speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance(t))}}async function run(q){setBusy(true);try{let r=await ai(q),m=r?.reply||"Done.";speak(m);done?.(m)}catch{speak("I couldn't complete that.")}finally{setBusy(false)}}function tap(){if(on){rec.current?.stop();setOn(false);return}let R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R){let q=prompt("Tell Rivetara what you need");if(q)run(q);return}let r=new R();rec.current=r;r.lang="en-GB";r.onresult=e=>run(e.results[0][0].transcript);r.onend=()=>setOn(false);r.start();setOn(true)}return <div className="ai"><button className={on?"listen":""} onClick={tap}>{on?<Square/>:<Sparkles/>}</button><div><b>{on?"Listening…":busy?"Working…":"Rivetara AI"}</b><small>{on?"Tap again to stop":"Tell Rivetara once. Forget the admin."}</small></div></div>}